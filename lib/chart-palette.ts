/**
 * จานสีกราฟ — spec §7 ห้ามใช้หลายสีในกราฟเดียว ให้เล่นเข้ม-อ่อนของฟ้า
 *
 * เป็น ordinal ramp ไม่ใช่ categorical: slice เรียงตามยอดจ่าย สลับลำดับแล้ว
 * ความหมายเปลี่ยน สีจึงต้องสื่อลำดับ = สีเดียวไล่เข้ม→อ่อน
 * ค่าไล่ใน OKLCH ไม่ใช่ hex lerp (lerp ผ่านโซนน้ำเงินจะออกม่วงหม่นกลางทาง)
 *
 * วัดจริงบนพื้นการ์ดกระจก (~#EDF6FC):
 *   #144c8c 8.9:1 · #1e60ad 6.2:1 · #2974cf 4.2:1
 *   #408be3 3.1:1 · #62a3e9 2.4:1 · #84b9ef 1.9:1
 *
 * สามขั้นอ่อนต่ำกว่า 3:1 และรัดยังไงก็ไม่ผ่าน — พื้นกระจกเกือบขาวไม่เหลือที่ให้
 * ไล่ 6 ขั้นที่ทั้งแยกออกจากกันและคอนทราสต์พอ จึงชดเชยตามที่ต้องทำ:
 *   1. เว้นช่อง 2px สีพื้นระหว่าง slice → ทุกชิ้นมีขอบ ไม่ต้องพึ่ง fill contrast
 *   2. label ตรง + ตารางจัดอันดับข้าง ๆ → ตัวตนมาจากข้อความ ไม่ใช่สี
 * ห้ามตัดข้อใดข้อหนึ่งทิ้ง ไม่งั้น slice อ่อนจะหายไปกับพื้น
 */
export const RAMP = [
  "#144c8c",
  "#1e60ad",
  "#2974cf",
  "#408be3",
  "#62a3e9",
  "#84b9ef",
] as const;

/** จำนวน slice สูงสุด — เกินนี้ค่าใกล้กันจนอ่านไม่ออก ที่เหลือยุบเป็น "อื่น ๆ" */
export const MAX_SLICES = RAMP.length;

/** spec §7 — เส้นกราฟใช้ accent-primary + gradient fill จางลงล่าง */
export const LINE = "#2e7fe0";

/** รายรับ mint / รายจ่าย primary — ห้ามแดง (spec §3) รายจ่ายไม่ใช่เรื่องแย่ */
export const INCOME = "#8fddd4";
export const EXPENSE = "#2e7fe0";

/** สีพื้นการ์ดกระจก ใช้เป็นช่องว่างระหว่าง slice */
export const SURFACE = "#edf6fc";

export const AXIS = "#4f6379";
export const GRID = "rgba(79,99,121,0.15)";
